defmodule Parser.Macros do
  # defmacro gen_funs(child_tags) do
  #     quote do
  #       IO.inspect(unquote(child_tags))
  #       #defp process_lines([%{tag: unquote(k), value: unquote(v)} | rest], parent_level, source) do
  #         #process_lines(rest, parent_level, %{source | unquote(v): unquote(v)})
  #       #end
  #     end
  # end
end
